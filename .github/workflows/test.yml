name: Go Test

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - "**.go"

jobs:
  test:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v2
      - id: set-matrix
        run: |
          changed_dirs=$(git diff --name-only main | grep '.go$' | xargs -I {} dirname {} | uniq | jq -R -s -c 'split("\n")[:-1]')
          echo "Changed directories: $changed_dirs"
          echo "::set-output name=matrix::$changed_dirs"

  build:
    needs: test
    runs-on: ubuntu-latest
    if: needs.test.outputs.matrix != '[]'
    strategy:
      fail-fast: false
      matrix:
        include: ${{fromJson(needs.test.outputs.matrix)}}
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: "^1.12.1"
      - run: go test ./${{ matrix.include }}
